node_modules/
dist/
.DS_Store
.env.local
*.varlock.lock
*.tsbuildinfo
env.d.ts
portuni.db
.firecrawl/

# Project-scoped MCP configs are no longer used by Portuni — connection
# lives in user-scoped ~/.claude.json (via the Settings UI). Block any
# stray .mcp.json from sneaking in alongside repo content.
.mcp.json

# Local Netlify folder
.netlify

# Brainstorming visual-companion artifacts
.superpowers/

# One-shot/throwaway scripts (delete after use)
scripts/oneshot/
apps/desktop/binaries/
apps/desktop/sidecar-deps/
*.bun-build
.*.bun-build

# Sandcastle loop (agent harness) runtime state
.sandcastle/logs/
.sandcastle/worktrees/
.sandcastle/node_modules/
