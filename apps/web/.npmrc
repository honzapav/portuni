# react-i18next declares i18next as a peer. The one installed copy lives in
# the root node_modules (the server and apps/server/shared/i18n use it too),
# and the web resolves it from there; without this npm would install a
# second copy here. Install the root first (npm ci), then apps/web.
legacy-peer-deps=true
